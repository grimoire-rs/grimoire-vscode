# TODO — all items below addressed on branch `feat/design-conformance` (2026-07-11)

- [x] !!! Design/implementation discrepancy — webviews rewritten against the handoff.zip mockups (1a–1g, 2a/2b): kind-tinted tiles/badges on `--vscode-charts-*` tokens, 2a scope rows, 1b empty/loading states, 1e sections, 720px reading column + 300px right rail, deprecation banner. `feat: align webviews with design handoff…` (3cc17db)
- [x] grim executable setting requires reload — could NOT reproduce: config is read fresh on every call and a config change alone re-invokes the new binary (~1s), proven by live integration tests. Behavior locked in `test: lock in live-reload behavior…` (1917e20). If it still happens for you, please capture exact repro steps.
- [x] Changelog describe? — resolved extension-side: a CHANGELOG tab appears when the artifact ships CHANGELOG.md (fetched via `fetch --path`, like README/logo). No grim change needed.
- [x] Skill content tab / clickable bundle boxes — DETAILS shows README when shipped (else source), CONTENT shows the raw artifact source, bundles get the design's "What's inside" boxes; boxes and rail members are clickable and switch to that artifact. (3cc17db)
- [x] via-bundle members — status `source` ("bundle: <repo>") is parsed; uninstall is disabled with a hint for bundle-provided members; defense-in-depth toast on grim's `kept-by-bundle`. (6d6b611)
- [x] Bundle uninstall error + notifications — bundles are removed via `grim remove bundle` (uninstall never accepted kind bundle); all action failures now surface as VS Code error notifications, the panel banner remains only for content-load errors. (6d6b611)
- [x] Metadata panel alignment — rail is fixed 300px flush right, reading column fills the rest, stacks under 900px. (3cc17db)
- [x] Scope-box widths + icons — 2a geometry (170px/150px columns) with root-folder/globe icons per row. (3cc17db)
- [x] Badge coloring — per-kind tints from the charts token palette with the design's translucent backgrounds (skill blue, rule yellow, agent purple, mcp green, bundle orange). (3cc17db)
- [x] !!! UI framework — adopted @vscode-elements/elements (buttons, textfield, selects, badges, progress); note: ../vscode-ocx has no webview UI at all, so "as in ocx" was interpreted as "equally lightweight". Menus stay hand-styled per the mockups. (3cc17db)
- [x] Invented scope badges in browse — removed; single installed box chip (scope icon + label + version) per the design, state managed in the details panel. (3cc17db, refined in 9719161)
- [x] Single click = temporary details, double click = permanent — one reusable preview tab retargeted on single click, promoted on double click or on any action inside it. (a6eaa29)
- [x] Card context menu — right-click menu with Open Details / install per free scope / Update / Uninstall (via-bundle aware) / Install specific version… / Pin / Copy repo path / Copy share link; shares one entry builder with the gear menu. (a6eaa29)
- [x] Share link — `vscode://grimoire-rs.grimoire/open?repo=…` deep link (URI handler + onUri activation) focuses the search view, seeds the query, opens the details panel; copy points in the context menu and a share button in the details header. (a6eaa29)
- [x] Details tab icon — marketplace logo icon + "Skill: name"-style tab title. (3cc17db)
- [x] Details panel lags on open — header/tabs render instantly from catalog data with a progress ring; tab title/icon are set at panel creation, so nothing swaps after load. (9719161, fe12189)
- [x] Versions panel — removed (redundant with the PACKAGE panel's tag list). (9719161)
- [x] Install/downgrade specific version — split buttons (button + chevron): "Install specific version…" runs a tag QuickPick from `grim describe` (manual input fallback) and pins `repo:tag`. (9719161, fe12189)
- [x] via-bundle header affordance — now a "Via Bundle" button (bundle identity in the tooltip) that opens the bundle's details panel. (e169c69)
- [x] Installed indicator in browse — design-2b bordered box chip (scope icon + label + mono version), one per card. (9719161)
- [x] Project/Global boxes always visible — both scope rows render permanently; a free scope shows "Not installed" + its own Install split button; installing into a project without grimoire.toml runs `grim init` then `grim add` (verified: add fails with exit 79 before any network without a config). (fe12189)
- [x] Reuse scope-box buttons with version dropdown — the standalone header Install ▾ is gone; each row carries the marketplace-style split Install (disabled when that scope already has it). (fe12189)
- [x] Updates/Project/Global ribbons not collapsible — they collapse/expand now (chevron rotates, state survives re-renders). (fe12189)
- [x] Browse dropdowns cannot select — root cause: vscode-elements selects fire non-bubbling, non-composed `change` events, so event delegation never saw them; selects now get direct listeners after each render. (fe12189)
- [x] Missing filters — installed view gained Kind and Scope (project/global) filters; browse keeps Kind/Registry/Installed. (fe12189)

- [x] Bundle "What's inside" boxes fully clickable — the whole member box (CONTENTS tab) and each rail Contents row are the click target (role=button/tabindex, Enter/Space via main.ts), not just the title; unresolved members stay inert. (65592e3)
- [x] Clickable tag badges → search — rail TAGS keyword chips are buttons; clicking one focuses Browse and seeds its search with the tag via a new `searchTag` message reusing the deep-link path (extracted `focusBrowseSearch`). No tag badges exist in the sidebar, so nothing to wire there. (65592e3)
- [x] Search refresh must not rebuild the search bar — sidebar renders as three regions (search / filters / results); a result update re-renders only results (filters only when their options change), and the focused textfield is never rebuilt, so mid-type results no longer eat keystrokes or the caret. (65592e3)
- [x] Search clear "x" button — vscode-icon (action-icon button) slotted into the textfield's content-after; visible only with text (class toggle, no rebuild), click clears + refocuses the field and runs the empty-query search. Added the `vscode-codicon-stylesheet` id the icon needs to render. (4bdc63a)

- [x] Verification/fix pass — full backlog swept (2026-07-11):
  - Correctness: `pinned` typed nullable (grim emits `pinned: null` for unlocked artifacts) and both `refRepo(item.pinned)` derefs guarded — an undeclared, unlocked status item is skipped instead of throwing; sidebar install into an unconfigured project now runs `grim init` before `grim add` like the details host (`runAction` takes step sequences); the global install-state file `$GRIM_HOME/state/global.json` is now watched alongside the per-project `.grimoire/state.json`. (1fb9614)
  - Private-registry lock — `grim context` registries gained the additive `authenticated` flag (absent on older binaries ⇒ false); browse cards whose registry host is authenticated render a codicon-lock before the registry line, which now shows host + first org segment (`ghcr.io/grimoire-rs`). (54d9705)
  - Wiring + UX + polish — virtualWorkspaces declared unsupported; details deserialize re-asserts webview options; collapsed sidebar sections persist across view hide/show (vscode.setState); the card gear menu gained an Update entry for outdated installs; installed-chip leading check, 8px card-meta gap, varied skeleton widths + a "Refreshing from <host>…" loading footer, muted floating-tag note on floating (pinned:null) update rows, 7px details status dot, neutral-foreground active-tab underline, neutral filter-chip fallback. (e335b9d)
  - Three more review MINORs — the global-state watcher now `mkdirSync`s `$GRIM_HOME/state/` before arming (a fresh grim home has no state/ dir yet, so the non-recursive watcher was dead on arrival); the private-registry lock glyph no longer fires for every card on a stored credential for the public default registry (ghcr.io) — it also requires the card's host to differ from `context.default_registry`; `searchArgs` inserts a `--` separator before the positional query so a tag-chip/typed term that looks like a flag (e.g. `--foo`) parses as positional instead of erroring. (3f068c2)
- [x] Details header shows "vlatest" for floating-`:latest` installs — `installs[0]?.version` is the *declared* ref tag ("latest" for a floating install), which was shadowing the concrete `vm.latestVersion` grim already resolved. New pure `concreteVersion()` helper (model.ts) skips only the literal "latest" (matching grim's own `pick_highest_version`) and falls back to it when nothing concrete is known anywhere; used for the header badge only — scope rows, installed chips, and card badges still show the declared `:latest` tag as designed. (165c4d2)

- [x] Details panel UX overhaul (6 user-requested items, 2026-07-11):
  - Redundant scope status caption removed — the "Not installed / Installed in …" line below the Project/Global boxes duplicated the per-box state, so it's gone; the in-box states stay. (5ffe9b4)
  - No layout shift on load — the skeleton now renders the FULL page structure (scope boxes with real cached install state or, on a first-ever open, pending spinner shells; the tab strip; the 300px rail with placeholder panels; a reserved version slot) so header geometry and the rail column hold their shape as the real VM fills in. ScopeService caches its last snapshot for the instant skeleton. Tab decision: CONTENTS is the one tab guaranteed for every kind and renders in the skeleton; DETAILS/CHANGELOG are additive (item 5 makes DETAILS README-gated, so it can't be pre-reserved without risking a later removal) — they only ever appear, never disappear, shifting CONTENTS rightward when they do. (93c57c5)
  - Preview-tab marker — VS Code exposes no API to italicize a webview tab title (only `title`/`iconPath`; the `preview` flags belong to TabInputText/Notebook, not createWebviewPanel), so single-click preview tabs appended " (Preview)", dropped on promote/retarget. (006d324) Superseded per user feedback (2026-07-12): the preview tab title now renders in Unicode mathematical sans-serif italic glyphs instead (pure `unicodeItalic()` in model.ts; plain title on promote) — visually italic, with the known trade-off that tab-picker search can't match the styled part (artifact names are ASCII, coverage complete). Real preview semantics (system-wide tab reuse, double-click-header pinning) remain API-impossible for webviews; the extension keeps its own single-click retarget + in-panel promote emulation. (152ef99)
  - Header version chip — restyled to the kind-badge/registry-chip geometry (blue badge tokens, no lone pill), moved to last in the badge row so its late arrival never displaces earlier elements, with a reserved same-size skeleton placeholder. (aff53b2)
  - Tab semantics realigned across kinds — CONTENTS = the artifact's own content (source markdown for skills/rules/agents, the JSON descriptor for mcp, member boxes + raw manifest for bundles), always present; DETAILS = the shipped README only, shown when present and leading as the default, else CONTENTS is first. Unified the old CONTENT/CONTENTS split and dropped the fenced-JSON wrapping. grim will grow a README channel per kind separately — code is written against "readmeMarkdown present or null". (13c97dd)
  - MCP JSON presentation (its new CONTENTS home) — the JSON block fills the full reading column (horizontal scroll only when a value is genuinely too wide) and is syntax-highlighted by a tiny pure tokenizer (keys/strings/numbers/booleans/null/punctuation → VS Code debug-token colors), everything through esc() so hostile string contents stay inert. (e5f9f95)

- [x] Description companion (`__grimoire`) wired into the details panel — `buildVM` fetches the reserved companion tag once, in parallel with describe + the main fetch (no added latency), and reuses that one report for README, logo, AND changelog: README wins over the in-tree README (both feed the README-gated DETAILS tab from 13c97dd), logo wins over the in-tree logo, and CHANGELOG.md wins over the in-tree changelog (logo + changelog probe the companion's files[] and only fire an extra `--path` fetch when it lists the asset — the companion itself is fetched just once). A miss (no companion published, or the companion lacks that asset) is a clean, silent fallback — never an error/notification. The tag literal (`__grimoire`; user chose it over an OCI-invalid `.grimoire`) + `descRef()` (strips any user tag first, so `repo:1.2.3` → `repo:__grimoire`) live only in `grim.ts`. Relative image refs in a companion README stay unresolved (ponytail-noted ceiling: per-asset fetch + data: URI rewrite). Tests: README/logo/changelog each win over in-tree, each miss falls back with no error, hostile desc README stays escaped through the existing markdown-it `html:false` path, `descRef` unit. (feat/design-conformance)

- [x] Details tab body aligns with the header text column — the reading column (tabs + panels) now indents its left edge to the header title block (right of the 96px logo), not flush with the logo's left. CSS-only: header padding/gap/icon and the body indent all derive from one shared `--text-column-x` (edge-pad + logo + gap) in `details.css`, so no duplicated magic numbers. The mockup (1c/1d) keeps the body at the 44px gutter; user asked to indent regardless. Under 900px the indent collapses back to the 44px gutter so the stacked narrow layout stays readable. (feat/design-conformance)

- [x] Scope-box action rework (user, 2026-07-11) — each scope row's actions cell is now gear FIRST, then exactly ONE split button: Install ▾ (not installed) or Uninstall ▾ (installed; chevron menu = "Switch to specific version…" → the existing pick-version flow, which covers downgrades by pinning an older tag). The old disabled-Install/inline-Update/secondary-Uninstall trio is gone; Update lives only in the gear menu (still emitted when outdated, version-cell hint dot unchanged). Both split buttons share the primary style and a fixed min-width (5.5em) inside a reserved 138px flex-end actions cell, so Project/Global rows align — including the via-bundle case, whose chips render left of the (fully disabled) split button. Gear on both rows: not-installed menu = Copy repo path; installed = Update (when outdated) + Copy repo path. Review found 0 blockers/majors; the one MINOR (chips pushed the via-bundle button out of line) is fixed; consciously left: skeleton width reserve covers gear+button only, a via-bundle row's chips can still widen it on resolve (comment scopes the claim honestly). (fdca4bf, 283eea8)

- [x] Tag click misses the artifact itself (user, 2026-07-11) — clicking a TAGS chip on e.g. the grim MCP searched its keyword ("fetch"/"render") but the artifact wasn't in the results. Diagnosis: grim's matcher DOES search keywords+summary, but the default catalog source is the package index (phone book), whose schema carried neither — `IndexPackage.into_entry` hardcoded empty keywords/None summary, and `grim publish` announce never wrote them to metadata.json. Fixed in grim (PR #33, pushed): announce reads `com.grimoire.keywords`/`com.grimoire.summary` back from the just-published manifest via a new shared `keywords_from_annotations` seam (also deduplicates the two pre-existing inline parses) and writes both omit-empty into the index pointer — a package without them emits byte-identical metadata.json; the index consumer parses both additively (`#[serde(default)]`, old files parse unchanged, old grims tolerate the new keys). Verify gate green (1617 unit + 518 acceptance), review clean (0 findings above NIT). Caveat: hosted index.grimoire.rs entries only carry the fields after packages re-announce and the index recompiles — grim is forward-ready, the existing grim-MCP entry stays keyword-less until its next publish. (grim 176e892 + f47d96f)

- [x] Split-button width + version chip polish (user, 2026-07-11) — the 5.5em min-width (60.5px at the 11px row font) was smaller than "Uninstall" needs (~72px), so it never engaged and the button grew with its label; now 7em, both labels render one width (reserved 138px cell still fits). Header version chip: dropped the odd `v` prefix and swapped the loud blue `--vscode-badge-*` tokens for the neutral toolbar-chip look of the registry chip beside it. Two version tests re-anchored to the exact header-chip span (a bare "latest" substring check would collide with the scope rows' legitimate floating-tag text). (1f64bbb)

- [x] Via-bundle scope rows: Bundle button (user, 2026-07-11) — the disabled Uninstall split + "Via Bundle" chips are replaced by the third button variant: gear + ONE plain "Bundle" button (package codicon inside, same primary style, all-corner radius, no dropdown) that opens the providing bundle's details; tooltip names every providing bundle and keeps the "uninstall the bundle to remove it" guidance (multiple providers → opens the first, ponytail-noted ceiling). All three variants share one explicit 120px min-width (the split total was already implicitly ~120, so Install/Uninstall are width-unchanged; the old main-segment-only 7em rule is gone, `.split-main` now flexes to fill). Dead code removed: viaBundleChips, .via-bundle-chip CSS, the disabled-split rules. Follow-up: the skeleton's `.scope-actions` reserve was 8px under the real gear+gap+button footprint (138 vs 146px) since the rework — now 146px, no shift on load. (ab9bf23, 7896b19)

- [x] Scope-button internal alignment (user, 2026-07-11) — labels left-aligned in all three variants; the Bundle button's package icon moved to the right edge (space-between), mirroring the split buttons' left-label/right-chevron geometry. (e057590)
- [x] Tab body width matches the header boxes (user, 2026-07-11) — left edges already aligned via `--text-column-x`; right edges didn't because the header description/scope boxes cap at 820px while the reading content capped at 720px. All three now share one `--content-width` (820px) var, deviating from the mockup's 720px measure per user preference. (1b61360)

- [x] Scope-row status columns (user, 2026-07-11) — the version cell is now three fixed sub-columns (52px mono version · 14px glyph · status text, cell widened 150→190px so "update available" never wraps); the empty version/glyph spans still render in the not-installed/pending states, so checks, dots, spinner and status text sit at identical x across Project/Global rows regardless of version-string width. (1feebad)
- [x] Header version chip sized like its neighbors (user, 2026-07-11) — the three badge-row chips use three different fonts (10px caps kind badge, 11px UI registry, 11px mono version), each with its own line box, so the pills came out different heights; one shared fixed-height rule (20px, box-sizing, 0/8px padding) now sizes all three, fonts only affect the text inside. (9ea7f33)
- [x] Browse installed-chip drops the version (user, 2026-07-11) — the card chip is scope-only (check + scope icon + Project/Global); the pinned version made it too long. Chip only ever rendered on browse cards, so nothing else changes; versions remain in the details scope box and the card title. Dead .installed-chip-version CSS removed; escaping test repurposed to assert the version is gone entirely. (9ea7f33)

- [x] DETAILS tab always visible (user, 2026-07-11) — the tab was README-gated (13c97dd), so artifacts shipping no README (neither in-tree nor a `__grimoire` companion) showed no DETAILS at all; it now always renders first for every kind, disabled + dimmed with a "No README available" tooltip when nothing ships, exactly the old behavior (leading, active, clickable) when a README exists. CONTENTS is the default when DETAILS is disabled; the loading skeleton pre-renders both tabs so the strip no longer reflows on load. Follow-up fix: the first cut used `pointer-events: none` on the disabled tab, which swallows the very tooltip the item asked for — replaced with the native disabled attribute (blocks the click by itself) + a hover guard + 0.55 opacity. (350ed55, 198e05b)

- [x] Description companion v2 migration + stale-while-revalidate cache (handover accepted 2026-07-12) — the extension adopts grim's reworked companion surface behind feature detection while the old path keeps working byte-identically: `'has_description' in describe` (CONTRACT AMENDMENT relayed to grim-side: the handover's `description: bool` collides with describe's existing `description: string|null` — the boolean must ship as `has_description`) selects between ONE `fetch --description` call with every companion member inline (README/logo/CHANGELOG extracted with zero `--path` follow-ups) and the legacy `__grimoire` probe (kept until the minimum supported grim ships v2, then `descRef`/`DESC_TAG` die). Details opens are now stale-while-revalidate: a persistent per-repo latest-only snapshot store (`detailsCache.ts`, sha1-named JSON under globalStorageUri/details-cache, atomic tmp+rename writes, pruned to newest 32 by mtime — bounded, no content-addressed pileup) paints instantly with fresh install rows, then a background check decides: live `describe` (authoritative for the manifest digest AND rail metadata, so new tags/deprecation/late-published companions are all caught) + a companion `--digest-only` probe; both digests matching + deep-equal describe → stop (zero downloads); metadata-only change → repost from cache with the refreshed describe; content change → full refetch, cache rewrite, single repost (no flicker). Revalidate failure over a cached paint keeps the cached view silently. New info actually used: CHANGELOG tab now always visible (disabled + "No changelog available" tooltip, same native-disabled pattern as DETAILS — no pointer-events:none); companion README AND changelog get relative image refs rewritten to data: URIs from the inline files (closes the old ponytail ceiling; v2 path only — the legacy path's ceiling deletes with the channel); markdown-it moved to a shared `createMarkdown()` factory whose validateLink additionally permits `data:image/svg+xml;base64,` (the default allowlist silently blanked SVG images — caught in review because the original test asserted the intermediate string, not `md.render`). Test strategy per user decision (no grim edits from this side anymore): unit + shell-stub contract tests + a live capability-probing suite against the snapshot binary `../grimoire/target/release/grim` (self-skips when absent; network checks gated behind GRIM_LIVE_NETWORK=1). Review pass found 1 crash (omit-empty `content` → TypeError → stuck busy spinner), the SVG hole, the stale-metadata blind spot, changelog missing the asset rewrite, and a non-atomic cache write — all fixed. Grim-side realism note: the warm path assumes `describe.digest` equals `fetch.digest` for the same ref; if they ever diverge the cache never short-circuits (correct, just no savings). (3196544, dec685e, 10aaa0b, 193815d, 05ee519, 5c4a2a1, 89d79a5)

- [x] Background-revalidate status indicator (user, 2026-07-12) — with stale-while-revalidate there was no way to tell the background check was running: a small top-right overlay in the details webview now shows a spinning loading codicon while revalidating, a check that holds then fades (~2s CSS keyframes + a JS state-clear so a later re-render can't resurrect a stale check) on completion, and — honesty state — a warning codicon titled "Refresh failed — showing cached data" that persists until the next successful cycle when revalidation fails over a cached paint. Cold opens show nothing (the paint itself is the signal). New `{type:'revalidate', state}` protocol message; pure `revalidateIndicator()` renderer; indicator host emitted in both render roots so content reposts don't wipe an in-flight state; pointer-events:none throughout (never blocks clicks). The failed branch is integration-tested deterministically (changed digest → content fetch errors → cached view kept + warning). (a9441c4)

- [x] Preview-tab marker: Unicode italic reverted to " (Preview)" suffix (user, 2026-07-12) — the math-italic glyphs render from a fallback font in the tab strip and look wrong; VS Code's italic preview tabs are workbench chrome for its own preview state, and webview panels expose only a plain-string title (no styling, no preview flag), so the textual suffix is the honest ceiling. `unicodeItalic()` and its tests deleted; titles still always rebuilt from repo/vm.name, never parsed back from panel.title. (f2bfbac)

- [x] Details→details opens respect preview semantics (user, 2026-07-12) — clicking an artifact inside a details view (bundle member, via-bundle Bundle button) opened a brand-new PERMANENT tab every time; it now routes through the same singleton preview-slot machinery as a sidebar single-click: target already open → reveal that tab; otherwise the preview slot navigates in place (or is created). One-line re-route in the openDetails message handler — the slot machinery already covered all three cases, and the existing stale-VM guard makes the in-place retarget race-free (repo swaps before the re-render, so in-flight posts for the old repo drop). The back/forward navigation history that was floated alongside this was DISCARDED by decision (2026-07-12): VS Code's native nav buttons have no extension hook, so any history would be a half-native parallel system with real complexity in the panel-lifecycle code for chains that are one hop deep in practice — the fixed open semantics keep the originating tab available as the natural "back". (9a0bbe7)

- [x] Browse view refreshes several times during one install (user, 2026-07-12) — two causes, two fixes in the watcher layer: the shared trailing debounce went 300ms → 1000ms (grim's install writes — toml early, network work, lock/state late — straddled 300ms quiet gaps, each gap ending in its own refresh; new rule = refresh only after a full quiet second), and the root cause, self-inflicted refreshes, is gone: every extension-initiated mutating action (sidebar/details action helpers, pickVersion, updateAll, initProject, pin) now runs inside a re-entrant `suspendWhile()` on Watchers — events fired by our OWN writes are dropped (the action's completion refresh reads the world fresh, so dropping is safe), read-only grim calls never suspend. One install = one refresh. debounceMs made injectable for tests (which also surfaced a latent trap: two pre-existing tests drive a 300ms-interval writer that a 1000ms debounce would perpetually reset). Known ceiling: an fs event delivered after resume (watcher latency > action window) can still cause one late refresh — add a resume-grace only if it surfaces. (2c29a58)

- [x] Browse prefetch + card logo pop-in (user, 2026-07-12) — browse results warm the details cache in the background so clicking an item paints from cache: top 24 repos of each (re)posted result list, filtered to cache MISSES only (freshness stays the on-open revalidate's job — prefetch never digest-probes), through a dedup'd queue with concurrency 6, pending cleared on new searches, no retries (a failed prefetch just leaves the normal cold path), fire-and-forget on dispose. Work per repo is the content pipeline extracted single-source out of buildPipeline (describe + fetch + companion → cache.save; no snapshot, no VM, no indicator). `grimoire.prefetchDetails` setting (default on) kills it for network-shy setups; cache ceiling raised 32 → 256 so prefetch can't thrash actively-viewed entries. Cards now show the cached logo (attr-escaped data: URI, codicon tile fallback; sidebar CSP already allowed img-src data:) and prefetch completions repost sidebar state debounced 500ms from the last ready payload — logos pop in with no loading flash and no fresh grim search. presentLogos() bounds enrichment reads to the cached subset (one readdir + ≤cache-size loads), not the catalog size — the thousands-of-skills strategy is bounded-K + uncached-only + capped fan-out. Installed view shows cached logos but doesn't prefetch (not a catalog browse). (2c0c81a)

- [x] Warm details open still lagged despite the cache (user, 2026-07-12) — the cached paint was gated on a FRESH `scopes.snapshot()` (3-5 sequential grim spawns for install rows) before the first post. Now: warm open with a last-known snapshot paints cached content against that STALE snapshot immediately (zero grim spawns before first paint — argv-order-tested), then exactly ONE fresh snapshot per open (threaded through revalidate, verified across all five paths) reposts only when the install-relevant slice actually differs. `snapshot()` itself also got its independent project/global chains Promise.all'd (intra-chain context→status order kept), cutting fresh-snapshot latency for every consumer. Remaining irreducible lag on FIRST opens is the VS Code webview process boot (~100-300ms, no API); retargeted preview panels skip it. Ceiling: warm-with-changed-content can post up to twice after the instant paint (install-diff, then content) — each post is correct data, not coalesced. (51bd0d7)
- [x] Resources rail icon grid (user, 2026-07-12) — RESOURCES rows' icons now sit centered in a fixed 16px column so labels start at one x (inline-flex gap let text drift with each glyph's advance); Installation/Package rows use label/value text pairs with no icon drift and were left alone. (5f0cfe2)
- [x] DETAILS tab renamed to README (user, 2026-07-12) — visible label only (enabled + disabled + skeleton); internal data-tab id stays 'details' (renaming ripples through panel ids/CSS for zero user gain); the disabled tooltip "No README available" finally matches its tab. (1fa26e0)

- [x] Clickable revalidate-failure detail (user, 2026-07-12) — the failed warning indicator now carries the CONCRETE failure (the error-envelope message from whichever pipeline step died) as its attr-escaped tooltip, and only in the failed state becomes interactive: click → VS Code warning notification with the full message + "Show Output" jumping to the channel. The message shown on click is looked up host-side per repo (webview text never trusted). Diagnosed live against the user's setup where it earned its keep immediately: the extension was spawning PATH grim (stale 0.8.4 ocx release without fetch/describe — exit 64) because `grimoire.path.executable` is machine-overridable and a WSL-remote window ignores the local User-settings value; the fix is setting it in Remote [WSL] or Workspace settings. (b6955b5)
- [x] Kind filter: dropdown → badge multi-select (user, 2026-07-12) — the browse (and installed — shared renderer, one change covers both) kind dropdown is now a chip row All · Skill · Rule · Agent · MCP · Bundle with kind codicons; selection semantics live in a pure `toggleKinds` reducer where the EMPTY selection IS "All" (All chip active exactly when empty): clicking a kind toggles it, clicking All clears, deselecting the last kind collapses to All, selecting all five collapses to All. Filtering stays client-side (CardFilter.kind → kinds[], union match); no protocol change was actually needed and no back-compat shim for stale persisted webview filter state (internal, pre-release). (b7692af)

- [x] Legacy `__grimoire` companion path deleted (user directive, 2026-07-12) — the extension is unreleased and pre-1.0 policy allows no compat shims, so the transitional probe path died ahead of schedule: `DESC_TAG`/`descRef`/`legacyCompanion` and their 7 tests are gone; the companion is fetched exclusively via `fetch --description` and only when `describe.has_description === true` (absent/false/null describe → no companion, zero probe spawns). In-tree README/logo/changelog `--path` fallbacks stay — content channel, not a shim. BREAKING for pre-v2 binaries: in-tree content only, and stale-binary errors (exit 64) surface loudly instead of silently falling back. Hostile-README XSS coverage carried by the createMarkdown factory + resolveCompanionAssets tests (integration duplicate dropped, not ported). (0a5388e)
- [x] Resolved-executable visibility (after the which-binary debugging round, 2026-07-12) — the output channel logs `grim executable: <path>` at activation and on config change, and every not-found/exit-64 failure line names the executable that was spawned (diagnostic lives in ScopeService.run, the single chokepoint all spawns go through). Root episode: `grimoire.path.executable` is machine-overridable, so a WSL-remote window ignores the local User-settings value — it must be set in Remote [WSL] or Workspace settings; the extension silently fell back to the stale PATH grim without fetch/describe. (b962ac6)

- [x] No-compat cleanup sweep (user directive, 2026-07-12) — grep-driven audit of every fallback/legacy/compat marker, each hit classified in the report: extension-side compat to DELETE = none survived (the prior batch removed the whole legacy path); stale compat FRAMING reworded (revalidate comment, grimLive exit-64 comment now names the no-shim design); forward-contract tolerances explicitly KEPT (frozen-additive `has_description?`/`authenticated?`/`reason?`, nullable-means-null, in-tree content channel, error-degrade paths — contract, not compat). Details cache gained a schema version (CACHE_VERSION=1): load() deletes missing/mismatched-version files, so pre-cleanup snapshots self-purge instead of surfacing as confusing intermediate states. `.serena/` gitignored. (f0aeabe)
- [x] 0.1.0 release payload (user, 2026-07-12) — CHANGELOG 0.1.0 section in user terms with a TODO-marked grim version floor ("newer than 0.8.4, first with the v2 interface"); README rewritten marketplace-facing (current tab names, kind chips, SWR/prefetch, prefetchDetails settings row, Requirements around the v2 floor, Screenshots placeholder slots); .vscodeignore fixed a REAL leak (tsconfig.test.json shipped; now tsconfig*.json) + TODO.md/handoff.zip/.serena excluded. Packaged vsix inspected: 215.61 KB, 16 files, no src/tests/coverage/docs, dist/webview + codicons present, vsce validation clean. Release infra (workflows/manifest/icon) was already in place; remaining steps are the user's (GitHub repo, marketplace publisher + Entra federated credential, secrets, then the grim v2 release → tag v0.1.0). (18e745c)

- [x] Card Update button oversized vs Install (user, 2026-07-12) — Install is a custom `.split-button.sm` (~20px) while Update is a `vscode-button` web component whose shadow control defaults to 28px height / 13px padding, so the two never matched. First cut (0ffb52d) pinned BOTH to new 22px/76px metrics — user correction: the intent was to shrink Update to Install's ORIGINAL size, not grow both. Second cut (8720a65) styled the web component's `::part(base)` to Install's recipe — still didn't match on the user's screen (shadow-DOM styling is version-fragile). Root fix (848fa57): the card cluster's Update/deprecated-Install stopped being `vscode-button` web components at all — native `<button class="card-btn">` carrying the split button's literal CSS recipe + 76px min-width (= the Install split's natural total: 51px main + 25px chevron segment) — identical height by construction, identical width by measure, nothing left to drift. `vscode-button` remains only outside the cards (banners). Details scope rows (3f77707, 24px) are a separate surface, untouched. (0ffb52d, 8720a65, 848fa57)

- [x] Registry filter removed from browse (user, 2026-07-12) — the dropdown gave no value: the host NEVER passed a registry to grim search (the `searchArgs` registry param had zero callers — dead code, deleted), so filtering was purely client-side cosmetics; search always spans all configured registries. Select markup + CardFilter.registry + wiring + tests deleted; the per-card registry host label and the footer registry counts stay (informational). No shim for stale persisted webview filter state. (4bdb42f)
- [x] Scope-row action button heights equalized (user, 2026-07-12) — Install ▾/Uninstall ▾ (split) and Bundle shared the 120px width but not height (12px chevron vs 14px package icon drove content-height apart); all three variants now pin to a fixed 24px height with centered content, gear centered by the existing row alignment. CSS-only, deterministic. (3f77707)

- [x] Scope dropdown → icon chips (user, 2026-07-12) — premise correction from implementation: browse has NO scope dropdown, and the install target is a UI-less heuristic (`defaultScopeFor`); the only scope select was the INSTALLED view's scope FILTER. That select is now a compact icon chip control (All · folder=Project · globe=Global, kind-chip styling, Project omitted when no workspace — the select's old guard), feeding `filter.scope` through the unchanged channel. Kept 3-state (not a binary toggle): the filter needs "All" or both sections could never show together. Dead select wiring/CSS removed. OPEN QUESTION for the user: if the actual ask was a browse-toolbar toggle that overrides the install TARGET for card installs, that's a net-new feature — not built yet. (18dbf65)
- [x] Installed view: collapsed sections pin to the bottom (user, 2026-07-12) — expanded sections are `flex: 1 1 0` with internally scrolling bodies, collapsed ones `flex: 0 0 auto` (header only), so a trailing collapsed header lands at the panel bottom and a leading one stays on top — Explorer/SCM idiom. CSS-only, container already flexed, browse untouched. (04a0353)

- [x] Chip-row group labels (user, 2026-07-12) — the stacked kind/scope chip rows read as floating badges; each row now carries a small caption (KIND above the kind chips in browse + installed, SCOPE above the installed scope chips): a `.chip-group-label` span with `flex-basis:100%` inside the existing flex-wrap row (no new containers), rail-title recipe scaled to 9.5px. Stacked-above chosen over inline-left because the six kind chips already wrap at sidebar widths. (364fde1)

- [x] Details open lag, layer 2: the webview reboot (user, 2026-07-12) — after the zero-spawn paint the residual lag was the retarget itself: every preview navigation reassigned `webview.html`, a full webview teardown (script re-parse, codicon reload, ready round-trip) burying the cached paint. Now `webview.html` is assigned exactly ONCE per panel (attach); retargets update repo/title/icon and swap content via messages into the live DOM (webview tracks currentRepo, resets indicator/scroll/serialized state on repo change — pure `shouldResetUi` decides; `render()` replacing #root resets tabs/menus for free). Plus first-paint inline skeleton: `webviewHtml` gained a bodyContent param and the initial document inlines the full skeleton (shared `skeletonVM` builder with postSkeleton, repo esc()-tested), so structure shows on first HTML parse before any script boots. `repoOf(panel)` confirmed as sole repo truth; stale-VM race guard unchanged. Integration test pins the invariant: a→b→c retargets keep `webview.html` byte-identical. Remaining ceiling: the VS Code webview process spawn on a panel's FIRST open — no API. (919d462)

- [x] Details scope row: Update takes precedence over Uninstall (user, 2026-07-12) — an outdated direct install now leads with an `Update ▾` split button (same 120px/24px geometry): main = update that scope, chevron menu = "Switch to specific version…" + "Uninstall" (both former affordances, one hop deeper). Up-to-date rows keep `Uninstall ▾`; not-installed (`Install ▾`) and via-bundle (`Bundle`) rows untouched. The gear drops its now-duplicate Update entry for direct installs (keeps Copy repo path; via-bundle rows keep gear-Update since their button is Bundle). Version-cell hint dot unchanged. (174c7f4)
- [x] Preview-tab promote affordances (user, 2026-07-12) — VS Code gives extensions NO event for tab-strip double-click or its italic preview styling (API wall, confirmed), so two in-panel substitutes: a `codicon-pin` "Keep open" header button rendered only while the panel IS the preview slot (`vm.isPreview`, stamped host-side at the single `postArtifact` choke point so retargets/refreshes always carry the current value — webview state never trusted), and body double-click (guarded by pure `isInteractiveTarget` so text/button/link/tab interactions never promote). Both post `promote` → host runs the existing sidebar-dblclick promote path → posts `promoted` back, which flips the pin off with zero grim round-trip. Known sub-frame gap: the inline first-paint skeleton lacks the pin until the first post stamps isPreview. (b77649f)

- [x] New logo set adopted (user, 2026-07-12) — assets copied verbatim from ../grimoire/assets with upstream names: `logo.png` (192px, marketplace gallery icon + details editor-tab icon), `logo-mono.svg` (activity bar — VS Code alpha-masks it, fill colors irrelevant), `logo.svg` (full-color, unreferenced but shipped). Old `grimoire.svg`/`logo-128.png` deleted; stale `.vscodeignore` whitelist dropped (assets/ was never excluded). vsix verified: all three inside, 16 files. (2a020db)

- [x] Installed view → native views split (user-decided over custom splitters, 2026-07-12) — the single installed webview became three real VS Code views (Updates / Project [`when: workspaceFolderCount != 0`] / Global), so resize, collapse, drag-reorder, and persistence come from the workbench for free; the custom section chrome (headers, collapse toggle, bottom-pin flex from 04a0353) and the installed SCOPE chips (18dbf65, partially retired) are deleted — scope is now which view you're in. Update All moved to a native view-title button on Updates (`$(cloud-download)`) and its command gained the smarter `projectConfigured()` gate from the old in-webview handler. Project/Global keep KIND chips + a client-side name search (which now actually filters, unlike the old catalog passthrough); native `WebviewView.badge` shows per-view item counts. Refresh fan-out consolidated: one shared snapshot + one search feeds all four views (was 2+2). Test seam: providers.installed → providers.{updates,project,global}. Known ceiling: installed descriptions enrich from the browse catalog cache and can be absent on a cold first paint until the next refresh. (1ed0a4b)
- [x] Browse INSTALL TO toggle (user confirmed 18dbf65's open question, 2026-07-12) — chip pair folder=Project / globe=Global under the KIND row (chip-group-label caption), exactly one active; seeded by pure `resolveInstallTarget` (explicit choice wins with project→global fallback when unavailable, else the old `defaultScopeFor` heuristic, which it replaces). Drives the card split-button MAIN Install action; chevron per-scope entries unchanged; Project chip disabled with tooltip when no configured workspace; choice persists in webview state. (b66816b)

- [x] Sidebar footer pinned to view bottom (user, 2026-07-12) — the "Cached catalog · synced … · N registries" line sat at the end of the scrollable results and drifted with content; it now lives in its own `#sb-footer` region under an internally-scrolling results area (`#root` 100vh flex column, results `flex:1; min-height:0; overflow-y:auto`, footer `flex:0 0 auto`) — pinned whether content is short or overflows, in browse and the split installed views alike. (ade27dd)
- [x] Selectable "Installed" browse chip removed (user, 2026-07-12) — the `toggle-installed` filter chip + `CardFilter.installedOnly` + its filterCards branch/handler/CSS deleted; per-card installed STATE indicators untouched. (26b5139)
- [x] Details body stretches to full height (user, 2026-07-12) — short content left `.details-body` content-tall, so the metadata rail's left border stopped mid-panel; `#root` is now a `min-height:100vh` flex column with `.details-body` `flex:1`, divider runs to the viewport bottom always. CSS-only; inline skeleton shares the structure so first paint doesn't jump. (681520e)

- [x] Concise menu labels (user, 2026-07-12) — "Switch to specific version…" → "Switch Version", "Install specific version…" → "Install Version" (not-installed rows — "Switch" reads wrong when nothing is installed), "Pin exact tag…" → "Pin Version"; no "…" suffixes or descriptor sublines in any webview menu (search-input placeholders and VS Code command-palette conventions untouched; the uninstall entry's "via <bundle>" hint stays — it names where, not a redundant descriptor). (77bf1d9)
- [x] Copy repo path dropped from details gear menus (user, 2026-07-12) — scope-row gears lose the entry; a gear left empty is hidden but its menu plumbing stays dormant for future entries. Kept where it earns its place: the details header repo path (click-to-copy tooltip) and the browse card context menu. (7d109e2)

- [x] Browse INSTALL TO toggle removed (user reversal of b66816b, 2026-07-12) — chip group, `set-install-target` wiring, and persisted state deleted; card main Install reverts to the pure heuristic (`resolveInstallTarget` collapsed back and renamed `defaultScope`: project when configured workspace open, else global); chevron per-scope entries unchanged. (b955222)
- [x] Project + Global fused into one Installed view (user, 2026-07-12) — the two scope views became a single `grimoire.installed` ("Installed", no `when` clause; container order Browse / Updates / Installed) whose toolbar carries the relocated toggle as a SCOPE display filter: folder=Project / globe=Global chips, exactly one active (pure `resolveInstalledScope`), webview-owned + persisted like the kind filter so toggling is instant with zero grim spawns. Project chip enabled whenever a workspace folder is open — unconfigured shows the Initialize banner; disabled only with no workspace (Global forced). View badge follows the SHOWN scope's count (webview posts `installedCount` on ready; Updates keeps its host-side badge). Providers seam {updates, project, global} → {updates, installed}. Known limits: badge round-trip has no integration test (harness lacks a WebviewView.badge double); scope choice survives webview reloads but resets to the heuristic on extension reactivation (same guarantee as the search query). (dd27bfd)

- [x] Details open lag, layer 3: the 250ms click timer (user, 2026-07-12) — the residual ~1s click→tab lag decomposed into (a) a sidebar-webview 250ms single/double-click disambiguation timer delaying EVERY card click before anything was even posted, and (b) the already-documented webview-process boot on a panel's first open (no API). (a) is deleted: single-click posts `openDetails` immediately; the timer existed only to keep a double-click from opening a preview first, but promote-in-place is seamless (no reload), so the double-click's first click opening the preview is harmless and its second click is now a host-side same-repo guard in `openPreview` (reveal-only, no retarget/repaint/revalidate churn — sentinel-title tested). Warm retargets are now skeleton-free instant paints with zero self-inflicted delay; SWR invariants unchanged (cached paint from stale snapshot, revalidate always runs on open). (uncommitted)
- [x] No skeleton flash on refresh (user, 2026-07-12) — every refresh (reload button, file-watcher event) posted `phase:'loading', items:[]` first, so the sidebar webview replaced painted cards with the skeleton rows and repainted when ready landed — the whole view visibly flickered. The webview now keeps the painted DOM when a loading state arrives over a ready/error paint (`keepPaintedOnLoading` in model.ts) and swaps only the bottom footer to the extracted `renderRefreshingFooter` ("Refreshing from <host>…"); the ready state that follows replaces results in place. Initial load and the no-grim screen still render the skeleton in full. Host untouched — the loading post stays as the first-paint path. (e2bbd9f)
- [x] In-place DOM patch, no wholesale region rewrites (user follow-up, 2026-07-12) — even with the loading state held, every ready post (refresh landing, each debounced `repostLogos` batch) rewrote `resultsEl.innerHTML` wholesale, repainting all cards to change one logo. The sidebar regions (filters, results, footer) now go through `patchRegion`: new HTML parses into an inert `<template>` and only subtrees whose serialized form changed are replaced — decision is the pure `patchStep` (model.ts): identical outerHTML = skip, identical shell (tag+attrs) with element-only children both sides = recurse, else replace the whole node (wholesale-only replacement so data-repo can never go stale against content under it). An unchanged reload touches zero DOM; one landed logo repaints one card tile; a filter toggle swaps the flipped chip only. Search row keeps its existing signature+focus-preserving rebuild (already never rebuilt on refresh). ponytail: position-keyed — child count change rewrites that region wholesale (the flicker cases are all same-shape); upgrade to keyed-by-data-repo reordering if card-order churn ever shows. (e2bbd9f; superseded same day by the lit-html port below — patchRegion/patchStep deleted, lit owns reconciliation now)
- [x] lit-html port, whole webview render layer (user, 2026-07-12) — render.ts rewritten as lit-html templates (all exports return TemplateResult/`nothing`; keyed `repeat()` card lists; MenuEntry data-* via `ifDefined`; `unsafeHTML` only for markdown-it + highlightJson's esc()-escaped spans); both webview entries render via `render(tpl, region)` with the hand-rolled diff shim (patchRegion/patchStep/nodeInfo/signature gating/focus save-restore) deleted; details UI state (activeTab/openScopeMenu/tagsExpanded) is now explicit and reset on retarget instead of relying on innerHTML wipes; the host inlines the first-paint skeleton by SSR-rendering the SAME templates (`@lit-labs/ssr` collectResultSync — attach stays sync, html assigned once); test emit moved from tsc-CJS to esbuild bundles (lit is ESM-only) with unit tests asserting on SSR strings via litString+normalizeHtml. UI-unchanged PROOF: 58 pre-migration goldens captured from the string renderers, parity.test.ts strictEquals lit output against them (59/59 green; normalizer tolerates only lit markers/whitespace/bool-attrs/attr-order). Orchestrated as 3 workflows (inventory → 4-phase implementation → adversarial 4-lens review); 1 confirmed review finding fixed (installed-view filter pipeline deduped into model.installedViewCards, shared by results template + badge count). 373 tests passing. Deliberate scope line: standalone lit-html, NOT LitElement componentization — delegation + host-pushed VMs stay (user confirmed; componentization would break DOM-level parity + global theme-token CSS). (d928ef8)

Post-implementation: two full review passes + one tail review (design conformance, correctness/security, race conditions) — all findings fixed (f341fb7, bf9f225), plus a follow-up MINOR-fix pass (3f068c2), a header-badge fix (165c4d2), and the details-panel UX overhaul above. 310 tests passing (+1 network-gated pending).

Known deliberate deviations: per-kind editor-tab glyphs (VS Code needs image URIs; the logo icon you suggested ships instead) · MCP tint uses the charts green token (no teal token exists) · deprecated cards lose the split chevron per the 1g state matrix · bundle scope-section cards still read "N scope(s)" rather than the mockup's "N artifacts" — the sidebar builds cards from search + status and never fetches a bundle's members document, so a bundle's declared artifact count isn't reliably derivable in that view (installed-members-via-this-bundle-in-scope is not the same as the bundle's artifact count once members are shared across bundles).













- [x] Feedback via native VS Code surfaces (user, 2026-07-12) — the details panel's top busy-bar/error-banner (which shifted the whole page) are gone: long grim runs show a refcounted spinning status-bar item (`Grimoire: <title>`, click → the Grimoire output channel; all four withProgress(Notification) sites converted), and every error path routes through `notifyError` — a popup with OPT-IN dedupe (5s window, background sources only: the three-view catalog-error burst and watch-driven revalidation collapse to one toast; a distinct user action's failure always shows). Cold details-load failure renders an in-body `.error-state` in the reading column (header never moves); warm revalidate keeps the top-right indicator + deduped toast. Adversarial review caught two majors before commit: the dedupe originally swallowed identical errors from distinct user actions (made opt-in), and `updateAll` discarded its GrimResults entirely (now checks per scope + notifies).
- [x] One catalog status line, live timestamp (user, 2026-07-12) — first landed as an Installed-ribbon footer, then per user correction ("always visible, not in a ribbon") moved OUT of the webviews entirely into a status-bar item (`src/catalogStatus.ts`); all sidebar footer code deleted. `grimoire.statusBarStyle` picks the render: `short` (default) = `$(book)` + one status glyph (check / spin / warning), `long` = `$(cloud) Catalog Cache · synced <rel>`; the tooltip always carries the full status (sync age, registry count, last failure) and click opens the Grimoire output (an earlier click=refresh binding surfaced "Webview is disposed" from a disposed details panel mid-refreshAll — root-caused and guarded via a disposedPanels WeakSet in details.ts). Hidden until there's something to report (first sync or first failure). Timestamp ages on a host-side 30s tick; the refreshing spinner still waits 1s and overlapping refreshes are refcounted (adversarial-review catch) so the LAST one settles the spinner. The Browse provider feeds `catalogSynced` directly — covers the startup path that never runs refreshAll (second review catch).
- [x] Resource hover underlines text only (user, 2026-07-12) — resource-row labels wrapped in `.resource-label`; the details.css hover rule targets the label span, so the codicon stays un-underlined.
- [x] Activity-bar badge counts only updates (user, 2026-07-12) — the Installed view's per-scope count badge rolled up into the activity-bar icon's number alongside Updates'; VS Code sums all view badges in a container, so the Installed badge (and its installedCount webview message) was removed outright. The Updates badge remains the only one — outdated count, cleared at zero.
